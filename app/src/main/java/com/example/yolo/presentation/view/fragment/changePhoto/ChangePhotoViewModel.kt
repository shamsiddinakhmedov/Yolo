package com.example.yolo.presentation.view.fragment.changePhoto

import com.example.yolo.presentation.architecture.BaseViewModel
import com.example.yolo.presentation.view.fragment.changePhoto.ChangePhotoViewModel.*

class ChangePhotoViewModel: BaseViewModel<State, Input, Effect>() {

    class Input
    class Effect
    class State

    override fun getInitialState(): State  = State()

    override fun processInput(input: Input) {
    }
}